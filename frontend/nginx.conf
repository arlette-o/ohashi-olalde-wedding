server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA fallback: React Router owns the URLs, so any path that isn't a
        # real file must serve index.html instead of nginx's 404.
        try_files $uri $uri/ /index.html;
    }
}
